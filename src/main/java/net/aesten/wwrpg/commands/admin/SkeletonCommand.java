package net.aesten.wwrpg.commands.admin;

import net.azalealibrary.command.CommandNode;

public class SkeletonCommand extends CommandNode {
    public SkeletonCommand(String name, CommandNode... children) {
        super(name, children);
    }
}
