package net.aesten.wwrpg.commands.admin;

import net.azalealibrary.command.CommandNode;

public class TpCommand extends CommandNode {
    public TpCommand(String name, CommandNode... children) {
        super(name, children);
    }
}
